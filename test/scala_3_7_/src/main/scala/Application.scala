object Application {

  // final case class Person(name: String, age: Int)

  // trait Config
  // object Config:
  //   def default: Config = ???

  // def implicitBased(implicit config: Config) = ???
  // def givenBased(using Config) = ???

  def main = {

    // implicitBased(Config.default)     

    // val person = Person("Alice", 30)

    // person match {
    //   case Person(name, a) =>
    //     println(s"Name: $name, Age: 30")
    // }
    println("Dance")
  }
}
