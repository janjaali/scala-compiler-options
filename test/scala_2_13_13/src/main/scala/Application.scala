object Application {

  def main(args: Array[String]): Unit = {

    // f(true, false)
  }

  // private def f(arg1: Boolean, arg2: Boolean) = arg1 && arg2

  // private def compare(x: Any, y: Any) = x match { case y => true }

  // private def printout(tree: Tree): Unit = tree match {
  //   case tree: Node => printout(tree.left); printout(tree.right)
  //   case tree: Leaf => println("Leaf")
  // }

  // sealed trait Tree
  // case class Node(left: Tree, right: Tree) extends Tree
  // case class Leaf(value: Int) extends Tree
}
