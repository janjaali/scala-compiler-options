object Application {

  def main(args: Array[String]): Unit = {

    // val someInt = 1
    // val someUnit = ()

    println("foo: " + {
      // math.pow(2, (someInt / 2) : Double)
      // someUnit.asInstanceOf[Int]
      // 42.isNaN()
      // f(42, 42)
    })
  }

  // private def f(u: Unit) = u
}
